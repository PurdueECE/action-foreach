name: "action-foreach"
description: "Run GitHub action on a list of repos."
author: "Purdue ECE"
inputs:
  repos:
    description: "List of repos to run action for"
    required: true
  foreach:
    description: "Action to run on each repo"
    required: true
runs:
  using: "docker"
  image: "Dockerfile"