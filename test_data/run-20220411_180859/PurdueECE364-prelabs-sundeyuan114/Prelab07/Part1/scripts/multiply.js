function myFunction() {
    var a = prompt("input first parameter to be multiply, 'a' :");
    var b = prompt("input second parameter to be multiply, 'b' :");
    alert ("This is what you entered: " + a * b);
  
  }

  myFunction();