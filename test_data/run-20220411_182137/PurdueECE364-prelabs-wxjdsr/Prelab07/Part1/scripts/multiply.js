function multiply(){
    var product = a * b;
    alert("The product of " + a + " multiply by " + b + " is " + product);
}

var a = prompt("Enter a number: ");
var b = prompt("Enter another number:");
// No need to call function since it will be called using "onclick" in html.